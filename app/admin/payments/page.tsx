import Link from 'next/link'
import {requireAdmin} from '@/lib/admin'
export default async function Payments(){const {supabase,admin}=await requireAdmin();const {data,error}=await supabase.from('registration_payments').select('id,user_id,amount,currency,payment_method,payment_reference,status,source,created_at,verified_at').order('created_at',{ascending:false}).limit(100);return <main className="page"><Link href="/admin">← Admin</Link><p className="eyebrow">TASKAURA ADMIN · PAYMENTS</p><h1>Registration Payments</h1><p className="muted">{admin.role}</p>{error?<div className="panel">Unable to load payments.</div>:<div className="list">{data?.map((p:any)=><article className="card" key={p.id}><div><strong>₹{Number(p.amount||0).toFixed(2)} · {p.status}</strong><span>{p.payment_method||'—'} · {p.source||'—'}</span><small>Ref: {p.payment_reference||'—'} · {new Date(p.created_at).toLocaleString('en-IN')}</small></div></article>)}</div>}<style>{`.page{min-height:100vh;background:#07070a;color:#fff;padding:24px 16px;max-width:1000px;margin:auto}.page>a{color:#aaa;text-decoration:none}.eyebrow{margin-top:28px;font-size:10px;letter-spacing:.2em;color:#888}.muted,small{color:#999;font-size:12px}.list{display:grid;gap:10px;margin-top:22px}.card,.panel{background:#111117;border:1px solid #292933;border-radius:16px;padding:16px}.card span,.card small{display:block;margin-top:5px;color:#999;font-size:12px}.panel{margin-top:20px}`}</style></main>}