import Link from 'next/link'
import {requireAdmin} from '@/lib/admin'
export default async function AdminSupport(){const {supabase}=await requireAdmin();const {data,error}=await supabase.from('support_tickets').select('id,user_id,category,subject,priority,status,created_at,updated_at').order('updated_at',{ascending:false}).limit(100);return <main className="page"><Link href="/admin">← Admin</Link><p className="eyebrow">TASKAURA ADMIN · SUPPORT</p><h1>Support Tickets</h1>{error?<div className="panel">Unable to load tickets.</div>:<div className="list">{data?.map((t:any)=><article className="card" key={t.id}><strong>{t.subject}</strong><span>{t.category} · {t.priority} · {t.status}</span><small>User {t.user_id} · {new Date(t.updated_at||t.created_at).toLocaleString('en-IN')}</small></article>)}</div>}<style>{`.page{min-height:100vh;background:#07070a;color:#fff;padding:24px 16px;max-width:1000px;margin:auto}.page>a{color:#aaa;text-decoration:none}.eyebrow{margin-top:28px;font-size:10px;letter-spacing:.2em;color:#888}.list{display:grid;gap:10px;margin-top:22px}.card,.panel{background:#111117;border:1px solid #292933;border-radius:16px;padding:16px}.card span,.card small{display:block;margin-top:6px;color:#999;font-size:12px}.panel{margin-top:20px}`}</style></main>}