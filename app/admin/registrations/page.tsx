import Link from 'next/link'
import {requireAdmin} from '@/lib/admin'
export default async function Registrations(){const {supabase,admin}=await requireAdmin();const {data,error}=await supabase.from('users').select('id,username,registration_source,created_by_admin_id,activation_status,must_change_password,created_at').order('created_at',{ascending:false}).limit(100);return <main className="page"><Link href="/admin">← Admin</Link><p className="eyebrow">TASKAURA ADMIN · REGISTRATIONS</p><h1>Registrations</h1><p className="muted">{admin.role} · Admin-assisted and direct account records.</p>{error?<div className="panel">Unable to load registrations.</div>:<div className="list">{data?.map((u:any)=><article className="card" key={u.id}><strong>@{u.username}</strong><span>{u.registration_source} · {u.activation_status}</span><small>{u.must_change_password?'Password change required':'Password set'} · {new Date(u.created_at).toLocaleString('en-IN')}</small></article>)}</div>}<style>{`.page{min-height:100vh;background:#07070a;color:#fff;padding:24px 16px;max-width:1000px;margin:auto}.page>a{color:#aaa;text-decoration:none}.eyebrow{margin-top:28px;font-size:10px;letter-spacing:.2em;color:#888}.muted,small{color:#999;font-size:12px}.list{display:grid;gap:10px;margin-top:22px}.card,.panel{background:#111117;border:1px solid #292933;border-radius:16px;padding:16px}.card span,.card small{display:block;margin-top:5px;color:#999;font-size:12px}.panel{margin-top:20px}`}</style></main>}