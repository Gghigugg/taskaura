import Link from 'next/link'
import {requireAdmin} from '@/lib/admin'
export default async function Audit(){const {supabase}=await requireAdmin();const {data,error}=await supabase.from('admin_audit_logs').select('id,admin_id,action,module,target_id,reason,created_at').order('created_at',{ascending:false}).limit(200);return <main className="page"><Link href="/admin">← Admin</Link><p className="eyebrow">TASKAURA ADMIN · AUDIT</p><h1>Audit Logs</h1>{error?<div className="panel">Unable to load audit logs.</div>:<div className="list">{data?.map((a:any)=><article className="card" key={a.id}><strong>{a.action} · {a.module}</strong><span>Admin {a.admin_id} · Target {a.target_id||'—'}</span><small>{a.reason||'No reason recorded'} · {new Date(a.created_at).toLocaleString('en-IN')}</small></article>)}</div>}<style>{`.page{min-height:100vh;background:#07070a;color:#fff;padding:24px 16px;max-width:1000px;margin:auto}.page>a{color:#aaa;text-decoration:none}.eyebrow{margin-top:28px;font-size:10px;letter-spacing:.2em;color:#888}.list{display:grid;gap:10px;margin-top:22px}.card,.panel{background:#111117;border:1px solid #292933;border-radius:16px;padding:16px}.card span,.card small{display:block;margin-top:6px;color:#999;font-size:12px}.panel{margin-top:20px}`}</style></main>}