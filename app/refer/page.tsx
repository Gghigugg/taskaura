import { createClient } from '@/lib/supabase/server'
import { redirect } from 'next/navigation'
export default async function ReferPage(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)redirect('/login');const {data:account}=await supabase.from('users').select('id').eq('auth_user_id',user.id).maybeSingle();const {data:profile}=account?await supabase.from('profiles').select('referral_code').eq('user_id',account.id).maybeSingle():{data:null};const {data:referrals}=account?await supabase.from('referrals').select('id,referred_user_id,status,qualified_at,reward_amount,created_at').eq('referrer_id',account.id).order('created_at',{ascending:false}).limit(50):{data:[]};return <main className="dashboard-shell"><section className="feature-card"><p className="eyebrow">TASKAURA</p><h1>Refer & Earn</h1><p>Invite eligible users with your referral code.</p><div className="balance-card" style={{marginTop:16}}><span>Your referral code</span><strong>{profile?.referral_code??'Not generated yet'}</strong></div><p className="muted">Rewards are credited only after server-side eligibility checks.</p><p>Total referrals: {referrals?.length??0}</p></section></main>}