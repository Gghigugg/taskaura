import { createClient } from '@/lib/supabase/server'
import { redirect } from 'next/navigation'
import Link from 'next/link'
export default async function SpinPage(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)redirect('/login');const {data:settings}=await supabase.from('spin_settings').select('enabled,entry_fee,daily_user_limit,campaign_start_at,campaign_end_at').eq('id',true).maybeSingle();const enabled=Boolean(settings?.enabled);return <main className="dashboard-shell"><section className="feature-card"><p className="eyebrow">TASKAURA</p><h1>Spin & Win</h1>{enabled?<><p>Spin is enabled by an administrator. Entry fee: ₹{Number(settings?.entry_fee??0).toFixed(2)}.</p><p className="muted">Play limits and campaign dates are controlled server-side. No outcome is guaranteed.</p><Link className="action-link" href="/spin-terms">Read Spin Terms</Link></>:<><p>Spin & Win is currently disabled.</p><p className="muted">It will remain unavailable until an authorized administrator enables it after applicable legal and payment-provider review.</p><Link className="action-link" href="/spin-terms">Read Spin Terms</Link></>}</section></main>}