import { createClient } from '@/lib/supabase/server'
import { redirect } from 'next/navigation'
import TicketForm from './ticket-form'
export default async function SupportPage(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)redirect('/login');const {data:account}=await supabase.from('users').select('id').eq('auth_user_id',user.id).maybeSingle();const {data:tickets}=account?await supabase.from('support_tickets').select('id,category,subject,priority,status,created_at,updated_at').eq('user_id',account.id).order('created_at',{ascending:false}).limit(50):{data:[]};return <main className="dashboard-shell"><section className="feature-card"><p className="eyebrow">TASKAURA</p><h1>Support</h1><p>Open a ticket and track your requests.</p>{tickets?.length?tickets.map(t=><article className="feature-card" key={t.id} style={{marginTop:10}}><strong>{t.subject}</strong><p className="muted">{t.category} · {t.status} · {t.priority}</p></article>):<p className="muted">No support tickets yet.</p>}</section><TicketForm/></main>}
