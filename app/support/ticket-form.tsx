'use client'
import {useState} from 'react'
export default function TicketForm(){const [v,setV]=useState({category:'general',subject:'',description:''});const [msg,setMsg]=useState('');const [busy,setBusy]=useState(false);async function go(e:React.FormEvent){e.preventDefault();setBusy(true);setMsg('');const r=await fetch('/api/support',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(v)});const d=await r.json();setBusy(false);setMsg(r.ok?'Ticket created successfully.':d.error||'Unable to create ticket.');if(r.ok)setV({category:'general',subject:'',description:''})}return <form onSubmit={go} className="feature-card" style={{marginTop:16}}><h2>Open a ticket</h2><select className="field" value={v.category} onChange={e=>setV({...v,category:e.target.value})}><option value="general">General</option><option value="account">Account</option><option value="task">Task</option><option value="payment">Payment</option><option value="withdrawal">Withdrawal</option><option value="security">Security</option></select><input className="field" placeholder="Subject" value={v.subject} onChange={e=>setV({...v,subject:e.target.value})} required/><textarea className="field" rows={5} placeholder="Describe the issue" value={v.description} onChange={e=>setV({...v,description:e.target.value})} required/><button className="action-button" disabled={busy}>{busy?'Sending…':'Create ticket'}</button>{msg&&<p className="muted">{msg}</p>}</form>}